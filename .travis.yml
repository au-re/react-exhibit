sudo: false
dist: trusty
language: node_js
node_js:
- 8
- 6
cache:
  directories:
  - node_modules
notifications:
  email: false
script:
- npm run check:staged
- npm run build
- npm run build:dist
branches:
  only:
  - master
